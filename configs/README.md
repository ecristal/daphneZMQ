Local runtime configs used by scripts in this repo.

`led_run_defaults.json` mirrors the SSP LED run defaults previously taken from
`~/repo/pds/configs/vd_coldbox/02_run_defaults.json` so remote hosts such as
`np04-srv-017` do not depend on a separate checkout.
